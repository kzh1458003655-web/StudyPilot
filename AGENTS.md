# StudyPilot Agent 工作入口

StudyPilot 当前采用 Harness 驱动的接力开发方式。仓库的第一优先级是按照既定架构完成重构；只有架构基础链路通过验收后，才允许继续实现产品功能。

## 当前状态

- `HARNESS-001`、`ARCH-001` 至 `ARCH-004`、`DB-001`、`INT-001`、`PROD-001` 至 `PROD-003` 已验证完成，下一项唯一可执行任务是 `VERIFY-001`。
- `ARCH-001` 已将课程版固定在 `archive/legacy-course-prototype` 分支和仓库外 worktree，现状盘点见 `docs/关键记录/现状盘点-ARCH-001.md`。
- 后续会话从 `VERIFY-001` 的系统级构建、端到端、隔离、异常和模型质量验证开始。

## 每次开始工作

1. 在仓库根目录运行 `./scripts/harness-init.ps1`。
2. 完整阅读本文件。
3. 阅读 `docs/harness/feature_list.json`、`docs/harness/progress.md` 和 `docs/harness/session-handoff.md`。
4. 根据当前任务阅读对应的产品、架构和关键记录文档。
5. 查看 `git status --short --branch` 和最近提交，确认不会覆盖其他人的工作。
6. 一次只选择一个 `ready` 任务，并在它自己的分支上工作。

初始化或基线验证失败时，先记录失败和影响，不得直接扩大任务范围或宣称功能完成。

## 文档事实来源

- 产品目标：`docs/产品相关/`
- 后端目标：`backend/docs/后端框架设计.md`
- 前端目标：`frontend/前端框架设计.md`
- 跨模块架构、数据库、接口和决策：`docs/关键记录/`
- 任务状态、进度、验证和交接：`docs/harness/`
- 名称含 `-旧` 的文档：冻结归档，只读，不再更新

若文档发生冲突，先在 `docs/关键记录/决策记录/` 创建 ADR，说明冲突、备选方案和建议；在决策明确前不得自行改变产品或架构基线。

## 工作范围

- `feature_list.json` 是任务状态的机器可读来源。
- 同一时间最多存在一个 `in_progress` 任务。
- 依赖项全部 `verified` 后，任务才可以进入 `ready`。
- `done` 只表示实现结束；有完整验证证据后才能标记 `verified`。
- 不得通过删除任务、修改验收标准或弱化测试来掩盖未完成工作。
- 验收标准需要变化时，先记录 ADR，再更新任务和受影响的正式文档。
- 架构任务未全部验证前，产品任务必须保持 `blocked`。

## 分支、Commit 和 PR

- 新任务必须创建分支；一个分支只处理一个任务或一个完整模块。
- 一个功能模块对应一个 PR，不把其他模块、批量格式化或旧代码清理混入其中。
- Commit 格式固定为 `type(scope): 中文动词 + 对象`。
- 允许的类型：`feat`、`fix`、`refactor`、`docs`、`test`、`chore`、`build`。
- `scope` 必填，使用稳定模块名，例如 `harness`、`architecture`、`backend`、`frontend`、`database`、`ai`、`qa`、`exam`、`assessment`。
- 摘要使用明确动作，不以句号结尾；一个 commit 只表达一个完整意图。
- 架构和数据库提交的正文必须说明原因、影响、验证方式和回退方式。

示例：

```text
docs(harness): 更新会话交接说明
refactor(backend): 建立资料模块边界
docs(database): 记录项目表外键策略
test(qa): 增加无依据拒答验证
```

## 旧代码复用

- 当前目标工作目录不得长期保留一套平行旧代码。
- 旧代码仅从 `archive/legacy-course-prototype` 的仓库外 worktree 读取。
- 不整目录复制旧实现；优先复用已有测试、纯算法、业务约束和已验证行为。
- 复用前补充行为特征测试，并在 `docs/关键记录/重构复用清单.md` 记录旧位置、目标模块、调整原因和验证结果。
- 架构耦合严重的实现只作参考，不强行迁移。

## 文档和数据库记录

- 每次只更新受当前任务影响的有效文档；每个阶段结束时执行全量一致性检查。
- 关键设计和取舍不能只写在 `progress.md`，必须进入 `docs/关键记录/`。
- 数据库变化必须同时更新 SQL、数据库演进记录、迁移或回退说明和验证证据。
- 不得提交数据库密码、项目密钥、真实连接地址、模型密钥或个人资料内容。

## 进度同步

- 开始任务时说明当前目标和预计验证方式。
- 完成重要阶段、发现阻塞或改变方案时及时同步。
- 结束前更新进度、任务状态、正式决策、验证证据和会话交接。
- 下一位 Agent 必须能够只依赖仓库记录继续工作，不需要重新猜测上下文。

## 完成和交接

结束会话前必须：

1. 运行 `./scripts/harness-check.ps1` 和当前任务要求的验证。
2. 将命令和结果写入 `docs/harness/verification.md` 或任务证据。
3. 更新 `progress.md`、`feature_list.json` 和 `session-handoff.md`。
4. 将关键架构、接口或数据库决策同步到 `docs/关键记录/`。
5. 确认工作区处于可安全继续的状态，并创建符合规范的小步 commit。

没有可复现的验证证据，就不能宣称任务完成。


