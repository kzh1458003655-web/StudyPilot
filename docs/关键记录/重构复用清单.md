# 重构复用清单

本文件记录旧课程版实现中可复用的行为、测试和纯逻辑。所有条目都必须在 `ARCH-001` 中重新检查；“候选”不代表允许直接复制到目标架构。

## 复用规则

1. 先建立旧行为的测试或输入输出证据。
2. 只复用符合目标模块职责的逻辑。
3. 不带入旧包结构、MySQL 方言、本机路径或跨层耦合。
4. 在目标模块中重新命名和组织，使其符合新架构。
5. 通过新旧行为对照后才能标记“已复用”。

## 初始候选

| 旧位置 | 候选价值 | 目标位置 | 当前结论 | 所需证据 |
| --- | --- | --- | --- | --- |
| `backend/src/test/.../CourseIsolationTest.java` | 数据隔离场景 | document、qa 和公共边界测试 | 待 `ARCH-001` 检查 | 现有测试结果和目标项目边界映射 |
| `backend/src/test/.../PlanValidationTest.java` | 时间和计划约束 | assessment 的复习建议或行为参考 | 待 `ARCH-001` 检查 | 约束清单及产品需求一致性 |
| `backend/src/main/.../CourseService.java` | 资料、课程和会话行为 | document、qa 服务 | 只复用行为，不复用旧结构 | 行为特征测试 |
| `backend/src/main/.../AiClient.java` | SSE、超时和模型调用经验 | ModelGateway 实现 | 待协议固定后重构 | 协议对照和错误测试 |
| `backend/src/main/.../PlanService.java` | 计划校验、事务和版本经验 | assessment 工作流参考 | 非核心模块，需产品确认 | 现有单元测试和目标需求映射 |
| `ai/src/main.cpp` | 检索、队列、取消和指标 | 独立 C++ 服务 | 可能高价值，禁止整文件盲搬 | 协议测试、负载和异常证据 |
| `frontend/app.js` | 已验证交互和错误场景 | 对应业务模块页面 | 只复用交互行为 | 浏览器测试和页面证据 |
| `scripts/` 与 `tests/` | 环境启动、集成和边界测试 | 新验证流程 | 优先复用可复现检查 | 干净环境运行结果 |

## 条目更新格式

```text
任务 ID：
旧位置与 commit：
旧行为：
目标模块：
处理方式：复用 / 重构 / 仅参考 / 放弃
不兼容点：
特征测试：
目标测试：
最终证据：
```

当前没有条目标记为已复用；正式盘点由 `ARCH-001` 完成。
