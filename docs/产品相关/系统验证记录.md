# 系统验证记录

日期：2026-09-09

## 已执行验证

| 范围 | 命令或路径 | 结果 |
| --- | --- | --- |
| 后端全量测试 | `backend\mvnw.cmd test` | 已执行；在补充测评级联删除后，针对性真实 PostgreSQL 测试通过。全量运行曾发现并定位级联删除缺陷，修复见 `a355bb0`。 |
| 数据库约束 | `backend\mvnw.cmd -Dtest=PostgreSqlSchemaIntegrationTest test` | 通过；两遍完整 DDL、资料、问答、组卷、作答、错题、掌握度、建议和项目删除均由 Embedded PostgreSQL 14.22 验证。 |
| 测评服务 | `AssessmentServiceTest` | 通过；单选规则评分、简答评分 JSON 校验、缺题拒绝和学习证据写入。 |
| 全流程 HTTP | `-Dstudypilot.e2e=true -Dtest=MockExamWorkflowEndToEndTest` | 通过；项目、PDF、组卷、独立作答与评分。 |
| 前端 | `pnpm run lint`、`format:check`、`typecheck`、`test`、`build` | 通过；包含资料、问答、真题、模拟卷和测评路由。 |
| 无数据库启动 | 端口 18084：`GET /api/v1/health` | 返回 `UP`。 |
| 外部依赖失败语义 | 端口 18084：`GET /api/v1/health/dependencies` | C++／模型未启动时返回 `EXTERNAL_SERVICE_UNAVAILABLE` 与 HTTP 503。 |

## 运行前置条件

完整资料导入、检索、模型出题与简答评分需要 C++ 服务和本地模型服务可用，并配置 `STUDYPILOT_DB_URL`、`STUDYPILOT_DB_USERNAME`、`STUDYPILOT_DB_PASSWORD`。只检查 Java 服务时无需数据库；其余业务接口不会装配。

上传资料默认目录为 `D:\大四课程设计\StudyPilot-runtime\uploads`，可由 `STUDYPILOT_STORAGE_ROOT` 覆盖。此次启动日志与测试记录位于 `D:\大四课程设计\StudyPilot-output`。

## 结论

目标架构的业务闭环已由隔离 PostgreSQL、受控网关的 HTTP 测试覆盖；本机此时未运行 C++／模型服务，因此依赖健康检查的 503 是预期的环境结果，不将其记为业务回归。
