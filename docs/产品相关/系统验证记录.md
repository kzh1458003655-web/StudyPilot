# 系统验证记录

日期：2026-09-09

## 已执行验证

| 范围 | 命令或路径 | 结果 |
| --- | --- | --- |
| 汇总验证入口 | `scripts\verify-target.ps1` | 通过；脚本在 Windows PowerShell 下运行，日志和摘要默认写入 `D:\大四课程设计\StudyPilot-output`。 |
| 后端全量测试 | `backend\mvnw.cmd test` | 41 项零失败，2 项需实际 C++／模型服务的 E2E 测试按显式开关跳过。此前定位的项目删除级联缺陷已由 `a355bb0` 修复。 |
| 数据库约束 | `backend\mvnw.cmd -Dtest=PostgreSqlSchemaIntegrationTest test` | 通过；两遍完整 DDL、资料、问答、组卷、作答、错题、掌握度、建议和项目删除均由 Embedded PostgreSQL 14.22 验证。 |
| 测评服务 | `AssessmentServiceTest` | 通过；单选规则评分、简答评分 JSON 校验、缺题拒绝和学习证据写入。 |
| 全流程 HTTP | `-Dstudypilot.e2e=true -Dtest=MockExamWorkflowEndToEndTest` | 通过；项目、PDF、组卷、独立作答与评分。 |
| 真实资料问答链路 | `-Dstudypilot.e2e=true -Dtest=GroundedQaEndToEndTest` | 2026-09-09 通过；Qwen3-4B Q4、C++ 索引服务、临时 PostgreSQL 与 Spring Boot 完成 PDF 导入、检索和带引用回答。 |
| 前端 | `pnpm run check` | 通过；包含 ESLint、Prettier、类型检查、Vitest 和生产构建。测评页面的组件测试覆盖加载试卷、开始作答、提交完整答案与评分报告显示。 |
| Harness | `scripts\harness-check.ps1` | 通过；任务状态、依赖关系和文档链接有效。 |
| 无数据库启动 | 端口 18084：`GET /api/v1/health` | 返回 `UP`。 |
| 外部依赖失败语义 | 端口 18084：`GET /api/v1/health/dependencies` | C++／模型未启动时返回 `EXTERNAL_SERVICE_UNAVAILABLE` 与 HTTP 503。 |

## 运行前置条件

完整资料导入、检索、模型出题与简答评分需要 C++ 服务和本地模型服务可用，并配置 `STUDYPILOT_DB_URL`、`STUDYPILOT_DB_USERNAME`、`STUDYPILOT_DB_PASSWORD`。只检查 Java 服务时无需数据库；其余业务接口不会装配。

上传资料默认目录为 `D:\大四课程设计\StudyPilot-runtime\uploads`，可由 `STUDYPILOT_STORAGE_ROOT` 覆盖。C++ 索引、启动日志、测试记录和验证摘要也默认位于 `D:\大四课程设计` 下的运行时与输出目录。启动、停止和配置步骤见 [部署与运行指南](../部署与运行指南.md)。

## 结论

目标架构的核心业务闭环已由隔离 PostgreSQL、受控网关的 HTTP 测试和真实本地模型资料问答测试共同覆盖。临时模型服务已在测试后停止；未运行时依赖健康检查返回 503 属于预期环境结果，不将其记为业务回归。浏览器级 Playwright 测试尚未建立，因此 `VERIFY-001` 仍处于验证中。
