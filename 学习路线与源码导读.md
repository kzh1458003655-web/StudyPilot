# 知序 StudyPilot：从运行到读懂源码

这份导读服务于三个人一起讲解和继续修改项目。建议先完整跑通一次，再按自己的分工阅读对应模块。项目不是重新训练模型，而是把本地 Qwen3.5-4B 接入一个受控的课程场景：资料先检索，再让模型基于证据回答；计划由模型提出，再由程序校验和保存。

## 1. 先跑通产品

1. 确认 `D:\StudyPilot-runtime` 存在，并检查 `config.local.json` 的 `RUNTIME` 路径。
2. 双击 `启动知序.cmd`，打开 `http://127.0.0.1:18080`。
3. 上传一份可复制文字的 PDF，等待“已添加”。
4. 新建对话并询问 PDF 内的概念，观察回答下方的资料名和页码。
5. 切到“复习计划”，填写目标、日期、天数和每日时间，生成后勾选一项，再写“减少任务量”调整计划，检查已完成项仍在。
6. 双击 `停止知序.cmd` 停止本地服务。

如果启动失败，依次访问 `http://127.0.0.1:18080/api/health`。`database`、`ai_service` 和 `model_ready` 均为 `true` 时，链路已经完整。

## 2. 系统为何分四层

浏览器只访问 Java 服务（18080）。Java 负责上传、数据库、会话、校验和引用落库；C++ 服务（18081）只做索引、检索和模型转发；llama.cpp（18082）加载 Qwen3.5-4B Q4_K_M；MySQL 保存业务数据。这样的分层让“模型会说话”与“数据可靠、计划合法”分开。

```text
Vue 浏览器 → Spring Boot /api → C++ 检索和模型网关 → llama.cpp → Qwen3.5-4B
                  ↓                    ↓
                MySQL               本地 JSON 索引
```

## 3. 问答链路：RAG 到底发生了什么

RAG 是“检索增强生成”，不是微调。上传时 Java 用 PDFBox 抽取每页文字，保存 PDF 和 `documents` 记录，并将页文字发给 C++。C++ 把页面切成带重叠的小块，记录词频和来源。提问时，Java 把问题交给 `/retrieve`；C++ 用 BM25 返回相关块；Java 再次核验这些 `document_id` 确实在 MySQL 中存在，随后把块作为证据交给模型，并要求只据此回答。回答结束后，消息和每条引用写入 `messages`、`citations`。

### BM25 的阅读方式

在 `ai/src/main.cpp` 中，`tokenize` 把中文保留为单字与相邻二元词，英文保留为小写词。`retrieve` 先计算每个词出现在多少块中，再按 BM25 评分。一个词出现在很少的块中，IDF 较高；词在一个块里重复出现会增加分数，但会逐渐饱和；很长的块会被长度归一化，避免一整页文字总是排第一。`Chunk` 里有 `doc/name/page/text`，所以结果可回到原资料页。

## 4. 计划 Agent 的边界

计划功能采用“工具调用 Agent”。`PlanService.generate` 给模型两个工具：`get_learning_context` 返回课程资料大纲、要求和已完成任务；`submit_plan` 只接受 JSON 任务数组。模型必须先读上下文再提交计划，最多修正五轮。

程序不把模型当作规则引擎。`PlanService.validate` 固定检查：任务数 1—60、日期落在开始日期起的天数内、标题和说明非空、每项至少 5 分钟、每天总时长不超过限额。调整时，事务锁住 `plans`，保留 `done=true` 的任务，仅删除和重建未完成任务；版本号变化时拒绝过时写入。这是数据库事务和 Agent 结合的核心展示点。

## 5. 数据库该怎样讲

从 `backend/src/main/resources/schema.sql` 开始读。`documents → citations` 管理来源；`sessions → messages → citations` 表示聊天历史与引用；`plans → tasks` 表示一份计划及其任务；`plan_revisions` 保存每次变更快照；`agent_events` 保留 Agent 用过的工具和校验反馈。每张表的属性只依赖于自己的主键，关联字段用外键表达，满足课程要求的 3NF 思路。`idx_messages_session` 和 `idx_tasks_plan_date` 分别服务于按会话加载历史、按计划和日期显示任务。

## 6. 按成员阅读源码

| 成员 | 首读文件 | 学到什么 | 可讲的演示点 |
|---|---|---|---|
| 前端与交互 | `frontend/index.html`、`frontend/app.js`、`frontend/style.css` | Vue 状态、Fetch、SSE 缓冲、异常提示 | 提问时文字逐字出现；来源卡片；勾选任务后进度变化 |
| Java 业务服务 | `ApiController.java`、`PlanService.java`、`schema.sql` | REST、PDFBox、事务、乐观锁、工具调用 | 调整计划不丢已完成项；删除资料后引用仍保存名称页码 |
| C++ 推理服务 | `ai/src/main.cpp`、`ai/build.cmd` | UTF-8 切块、BM25、读写锁、SSE 代理、GPU 串行队列 | C++ 返回带页码的命中；基准测试 GPU 生成速度 |

## 7. 重点函数地图

| 位置 | 作用 | 修改时先想什么 |
|---|---|---|
| `frontend/app.js:send` | 读取 SSE 并实时更新消息 | 事件块必须按空行切分，不能假定一次网络读取就是一条事件 |
| `ApiController.chat` | 问答总编排与引用保存 | 取消、异常、正常结束都要释放 `busySessions` 并写最终状态 |
| `PlanService.generate` | 调模型工具调用并开启保存事务 | 先校验后写库；旧计划不得在模型失败时被破坏 |
| `PlanService.validate` | 确定性计划规则 | 新规则应写在这里并补单元测试 |
| `main.cpp:add_document` | 页文字切块和本地索引持久化 | UTF-8 边界和块重叠不能随意删除 |
| `main.cpp:retrieve` | BM25 排序 | 改分词或参数后用真实资料比较命中页是否更合理 |
| `main.cpp:/stream` | 模型 SSE 转发 | 8GB 显存下保留生成互斥，避免并发推理失稳 |

## 8. 本地模型的现实含义

Qwen3.5-4B Q4_K_M 是量化后的 GGUF 文件。量化把部分权重用较少位数存储，换取显存占用和速度，代价是可能轻微降低效果。4060 8GB 可让当前模型大部分甚至全部层卸载到 GPU；项目使用一个 `generation_mutex` 让推理串行，同时允许最多四个请求排队。基准中全 GPU 卸载生成约 59.13 token/s，CPU 约 11.89 token/s。参数只在本机测试中有效，换电脑须重新测量。

## 9. 如何修改又不破坏项目

想增加资料类型，先改 Java 的上传解析与校验，再给 C++ 索引一个规范的 pages JSON；想更改问答格式，修改 Java system prompt 和前端展示，但不可去掉“证据不足就说明”的约束；想增加计划规则，先改 `validate`，为合法和非法情况各写一条 JUnit；想换模型，先确认 llama.cpp 支持的 GGUF 与显存，再调整 `config.local.json` 和上下文长度。任何修改后至少运行 `build.ps1`，再运行 `tests/integration.py` 和 `tests/edge_cases.py`。

## 10. 术语表

- **RAG**：在生成前检索外部资料并将其作为模型上下文。
- **BM25**：根据词频、逆文档频率和文本长度给检索块打分的经典算法。
- **SSE**：服务器持续推送事件的 HTTP 格式，用于逐字显示模型输出。
- **事务**：一组数据库更新要么全部成功，要么全部回滚。
- **乐观锁**：提交时比较版本，发现期间有人改动就拒绝覆盖。
- **GGUF / Q4_K_M**：llama.cpp 使用的模型文件格式 / 一种 4-bit 左右的量化方案。

## 11. 下一次组内学习建议

第一轮，每人只沿着自己的请求链读一遍并在浏览器上演示；第二轮，前端同学增加一个来源筛选显示，Java 同学新增一条计划校验和测试，C++ 同学调整一个 BM25 参数并记录命中差异；第三轮，把改动写入个人报告的“详细设计、实现与测试”部分。这样既能理解完整系统，也能保留个人可说明的工作成果。
